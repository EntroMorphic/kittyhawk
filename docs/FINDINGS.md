---
title: Findings
status: ground-zero (2026-05-01) — no measurements yet
companions: NORTH_STAR.md · docs/THESIS.md · docs/REMEDIATION_PLAN.md
---

# Findings

The running ledger of measurements and what they mean. Empty at ground zero.

The prior cycle's findings are preserved in `01MAY26_archived/docs/FINDINGS.md` (eleven axes covering accuracy, speed, inspectability, signature-as-address, multi-table composition, Fashion-MNIST and CIFAR-10 generalization, Go-position substrate-distance refinement, image-pipeline gate, underused-features sweep, and substrate-legal LSH cost characterization).

This document populates from zero as the rebuild's first measurements land. Axes added by date and by what they decide.

## Axis structure

Each axis records:
1. The question it answers.
2. The measurement that answers it.
3. What that measurement *cannot* be read as also showing.
4. The journal cycle (raw → nodes → reflect → synthesize → closeout) that produced it.

## Axes (none yet)

The first axis will be tier 1's regression-guard: every base-3 trit kernel produces bit-identical output to the prior cycle's tests. This is housekeeping, not a substrate-claim measurement, but it gates everything that follows.
